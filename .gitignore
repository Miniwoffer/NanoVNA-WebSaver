# Temporary and binary files
*~
*.orig
*.log
.*.swp
.DS_Store

# Editor and tool directories
.idea
.vscode
.ropeproject
tags

# Node is only ever used ad hoc, for the browser-driven checks; the
# application itself has no dependencies and no build step
node_modules/
package-lock.json
